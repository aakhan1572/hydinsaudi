{
    "builds": [{
        "src": "main/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime":"Python-3.9.7" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "main/wsgi.py"
        }
    ]
}